# Iris reference figure

The figure is generated directly from the committed `metrics.json` and `predictions.csv`. It includes the complete training trace, all confusion-matrix cells, and both held-out errors.

- `contract.md` records the supported claim and one-seed boundary.
- `provenance.json` records input and output checksums.
- `preflight.json` records PDF structure checks at README width.
- `final-size-716px.png` and `grayscale-716px.png` record the manual legibility and color-independence review.
